pipeline{
    agent any
    stages{
        stage('terraform'){
            steps{
                sh "whoami"
                sh "pwd"
                sh "/opt/homebrew/bin/terraform"
                
            }
        }
    }
}